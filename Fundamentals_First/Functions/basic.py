def greet():
    name = input("Enter your name")
    print(f"Hello, {name}!")

greet() # you can only call a function if it is defined
# also variables defined in the function can only be called inside the function
